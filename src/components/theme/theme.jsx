import ComponentPreview from "../../app/components/componentpage/ComponentPreview";

export default function Theme() {
  const themeCode = ``;

  const themePreview = (
    <>
    </>
  );

  return (
    <ComponentPreview
      title="Theme"
      description="Modern theme component built using React and Tailwind CSS."
      preview={themePreview}
      code={themeCode}
      fileName="Theme.jsx"
    />
  );
}
