import ComponentPreview from "../../app/components/componentpage/ComponentPreview";

export default function Menu() {
  const menuCode = ``;

  const menuPreview = (
    <>
    </>
  );

  return (
    <ComponentPreview
      title="Menu"
      description="Modern menu component built using React and Tailwind CSS."
      preview={menuPreview}
      code={menuCode}
      fileName="Menu.jsx"
    />
  );
}
