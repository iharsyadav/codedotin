import ComponentPreview from "../../app/components/componentpage/ComponentPreview";

export default function Select() {
  const selectCode = ``;

  const selectPreview = (
    <>
    </>
  );

  return (
    <ComponentPreview
      title="Select"
      description="Modern select component built using React and Tailwind CSS."
      preview={selectPreview}
      code={selectCode}
      fileName="Select.jsx"
    />
  );
}
