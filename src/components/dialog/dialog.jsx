import ComponentPreview from "../../app/components/componentpage/ComponentPreview";

export default function Dialog() {
  const dialogCode = ``;

  const dialogPreview = (
    <>
    </>
  );

  return (
    <ComponentPreview
      title="Dialog"
      description="Modern dialog component built using React and Tailwind CSS."
      preview={dialogPreview}
      code={dialogCode}
      fileName="Dialog.jsx"
    />
  );
}
