import ComponentPreview from "../../app/components/componentpage/ComponentPreview";

export default function Toast() {
  const toastCode = ``;

  const toastPreview = (
    <>
    </>
  );

  return (
    <ComponentPreview
      title="Toast"
      description="Modern toast component built using React and Tailwind CSS."
      preview={toastPreview}
      code={toastCode}
      fileName="Toast.jsx"
    />
  );
}
