import ComponentPreview from "../../app/components/componentpage/ComponentPreview";

export default function Stepper() {
  const stepperCode = ``;

  const stepperPreview = (
    <>
    </>
  );

  return (
    <ComponentPreview
      title="Stepper"
      description="Modern stepper component built using React and Tailwind CSS."
      preview={stepperPreview}
      code={stepperCode}
      fileName="Stepper.jsx"
    />
  );
}
