import ComponentPreview from "../../app/components/componentpage/ComponentPreview";

export default function Chat() {
  const chatCode = ``;

  const chatPreview = (
    <>
    </>
  );

  return (
    <ComponentPreview
      title="Chat"
      description="Modern chat component built using React and Tailwind CSS."
      preview={chatPreview}
      code={chatCode}
      fileName="Chat.jsx"
    />
  );
}
