import ComponentPreview from "../../app/components/componentpage/ComponentPreview";

export default function Slider() {
  const sliderCode = ``;

  const sliderPreview = (
    <>
    </>
  );

  return (
    <ComponentPreview
      title="Slider"
      description="Modern slider component built using React and Tailwind CSS."
      preview={sliderPreview}
      code={sliderCode}
      fileName="Slider.jsx"
    />
  );
}
