import ComponentPreview from "../../app/components/componentpage/ComponentPreview";

export default function Input() {
  const inputCode = ``;

  const inputPreview = (
    <>
    </>
  );

  return (
    <ComponentPreview
      title="Input"
      description="Modern input component built using React and Tailwind CSS."
      preview={inputPreview}
      code={inputCode}
      fileName="Input.jsx"
    />
  );
}
