import ComponentPreview from "../../app/components/componentpage/ComponentPreview";

export default function Chart() {
  const chartCode = ``;

  const chartPreview = (
    <>
    </>
  );

  return (
    <ComponentPreview
      title="Chart"
      description="Modern chart component built using React and Tailwind CSS."
      preview={chartPreview}
      code={chartCode}
      fileName="Chart.jsx"
    />
  );
}
