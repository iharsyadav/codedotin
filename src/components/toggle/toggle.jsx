import ComponentPreview from "../../app/components/componentpage/ComponentPreview";

export default function Toggle() {
  const toggleCode = ``;

  const togglePreview = (
    <>
    </>
  );

  return (
    <ComponentPreview
      title="Toggle"
      description="Modern toggle component built using React and Tailwind CSS."
      preview={togglePreview}
      code={toggleCode}
      fileName="Toggle.jsx"
    />
  );
}
