import ComponentPreview from "../../app/components/componentpage/ComponentPreview";

export default function Copy() {
  const copyCode = ``;

  const copyPreview = (
    <>
    </>
  );

  return (
    <ComponentPreview
      title="Copy"
      description="Modern copy component built using React and Tailwind CSS."
      preview={copyPreview}
      code={copyCode}
      fileName="Copy.jsx"
    />
  );
}
