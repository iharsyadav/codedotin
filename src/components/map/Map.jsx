import ComponentPreview from "../../app/components/componentpage/ComponentPreview";

export default function Map() {
  const mapCode = ``;

  const mapPreview = (
    <>
    </>
  );

  return (
    <ComponentPreview
      title="Map"
      description="Modern map component built using React and Tailwind CSS."
      preview={mapPreview}
      code={mapCode}
      fileName="Map.jsx"
    />
  );
}
