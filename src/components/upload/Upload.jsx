import ComponentPreview from "../../app/components/componentpage/ComponentPreview";

export default function Upload() {
  const uploadCode = ``;

  const uploadPreview = (
    <>
    </>
  );

  return (
    <ComponentPreview
      title="Upload"
      description="Modern upload component built using React and Tailwind CSS."
      preview={uploadPreview}
      code={uploadCode}
      fileName="Upload.jsx"
    />
  );
}
