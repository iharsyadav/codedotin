import ComponentPreview from "../../app/components/componentpage/ComponentPreview";

export default function Hero() {
  const heroCode = ``;

  const heroPreview = (
    <>
    </>
  );

  return (
    <ComponentPreview
      title="Hero"
      description="Modern hero component built using React and Tailwind CSS."
      preview={heroPreview}
      code={heroCode}
      fileName="Hero.jsx"
    />
  );
}
