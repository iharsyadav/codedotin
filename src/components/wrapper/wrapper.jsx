import ComponentPreview from "../../app/components/componentpage/ComponentPreview";

export default function Wrapper() {
  const wrapperCode = ``;

  const wrapperPreview = (
    <>
    </>
  );

  return (
    <ComponentPreview
      title="Wrapper"
      description="Modern wrapper component built using React and Tailwind CSS."
      preview={wrapperPreview}
      code={wrapperCode}
      fileName="Wrapper.jsx"
    />
  );
}
