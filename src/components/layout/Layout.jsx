import ComponentPreview from "../../app/components/componentpage/ComponentPreview";

export default function Layout() {
  const layoutCode = ``;

  const layoutPreview = (
    <>
    </>
  );

  return (
    <ComponentPreview
      title="Layout"
      description="Modern layout component built using React and Tailwind CSS."
      preview={layoutPreview}
      code={layoutCode}
      fileName="Layout.jsx"
    />
  );
}
