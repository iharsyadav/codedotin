import ComponentPreview from "../../app/components/componentpage/ComponentPreview";

export default function User() {
  const userCode = ``;

  const userPreview = (
    <>
    </>
  );

  return (
    <ComponentPreview
      title="User"
      description="Modern user component built using React and Tailwind CSS."
      preview={userPreview}
      code={userCode}
      fileName="User.jsx"
    />
  );
}
