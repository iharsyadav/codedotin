import ComponentPreview from "../../app/components/componentpage/ComponentPreview";

export default function Grid() {
  const gridCode = ``;

  const gridPreview = (
    <>
    </>
  );

  return (
    <ComponentPreview
      title="Grid"
      description="Modern grid component built using React and Tailwind CSS."
      preview={gridPreview}
      code={gridCode}
      fileName="Grid.jsx"
    />
  );
}
