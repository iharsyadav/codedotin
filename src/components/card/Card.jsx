import ComponentPreview from "../../app/components/componentpage/ComponentPreview";

export default function Card() {
  const cardCode = ``;

  const cardPreview = (
    <>
    </>
  );

  return (
    <ComponentPreview
      title="Card"
      description="Modern card component built using React and Tailwind CSS."
      preview={cardPreview}
      code={cardCode}
      fileName="Card.jsx"
    />
  );
}
