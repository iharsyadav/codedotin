import ComponentPreview from "../../app/components/componentpage/ComponentPreview";

export default function Auth() {
  const authCode = ``;

  const authPreview = (
    <>
    </>
  );

  return (
    <ComponentPreview
      title="Auth"
      description="Modern auth component built using React and Tailwind CSS."
      preview={authPreview}
      code={authCode}
      fileName="Auth.jsx"
    />
  );
}
