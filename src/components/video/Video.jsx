import ComponentPreview from "../../app/components/componentpage/ComponentPreview";

export default function Video() {
  const videoCode = ``;

  const videoPreview = (
    <>
    </>
  );

  return (
    <ComponentPreview
      title="Video"
      description="Modern video component built using React and Tailwind CSS."
      preview={videoPreview}
      code={videoCode}
      fileName="Video.jsx"
    />
  );
}
