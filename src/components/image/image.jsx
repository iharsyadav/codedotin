import ComponentPreview from "../../app/components/componentpage/ComponentPreview";

export default function Image() {
  const imageCode = ``;

  const imagePreview = (
    <>
    </>
  );

  return (
    <ComponentPreview
      title="Image"
      description="Modern image component built using React and Tailwind CSS."
      preview={imagePreview}
      code={imageCode}
      fileName="Image.jsx"
    />
  );
}
