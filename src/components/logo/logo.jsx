import ComponentPreview from "../../app/components/componentpage/ComponentPreview";

export default function Logo() {
  const logoCode = ``;

  const logoPreview = (
    <>
    </>
  );

  return (
    <ComponentPreview
      title="Logo"
      description="Modern logo component built using React and Tailwind CSS."
      preview={logoPreview}
      code={logoCode}
      fileName="Logo.jsx"
    />
  );
}
