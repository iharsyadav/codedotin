import ComponentPreview from "../../app/components/componentpage/ComponentPreview";

export default function Navbar() {
  const navbarCode = ``;

  const navbarPreview = (
    <>
    </>
  );

  return (
    <ComponentPreview
      title="Navbar"
      description="Modern navbar component built using React and Tailwind CSS."
      preview={navbarPreview}
      code={navbarCode}
      fileName="Navbar.jsx"
    />
  );
}
