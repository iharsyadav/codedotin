import ComponentPreview from "../../app/components/componentpage/ComponentPreview";

export default function Spinner() {
  const spinnerCode = ``;

  const spinnerPreview = (
    <>
    </>
  );

  return (
    <ComponentPreview
      title="Spinner"
      description="Modern spinner component built using React and Tailwind CSS."
      preview={spinnerPreview}
      code={spinnerCode}
      fileName="Spinner.jsx"
    />
  );
}
