import ComponentPreview from "../../app/components/componentpage/ComponentPreview";

export default function Container() {
  const containerCode = ``;

  const containerPreview = (
    <>
    </>
  );

  return (
    <ComponentPreview
      title="Container"
      description="Modern container component built using React and Tailwind CSS."
      preview={containerPreview}
      code={containerCode}
      fileName="Container.jsx"
    />
  );
}
